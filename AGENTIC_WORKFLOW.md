# Agentic Workflow

이 문서는 moin 프로젝트에서 여러 역할의 Agent/Subagent를 사용해 작업을 진행할 때의 운영 규칙입니다.

## 목적

- `main` 배포 흐름과 분리된 브랜치에서 우선순위 작업을 순차적으로 개발한다.
- 사용자가 나중에 배포할 작업을 한 번에 보고 고를 수 있도록 작업 단위를 명확히 분리한다.
- `TODO.md`는 제품/개발 백로그로 유지하고, Agentic 진행 기록은 `AGENTIC_WORK_LOG.md`에 별도로 남긴다.

## 기본 원칙

- 기본 작업 브랜치: `codex/priority-roadmap-batch`
- 작업 폴더: `/Users/subong/Documents/moin`
- `main`에 직접 실험성 변경을 만들지 않는다.
- 배포는 사용자가 명시적으로 요청할 때만 진행한다.
- 각 작업은 가능한 한 작고 검토 가능한 단위로 나눈다.
- 변경 후에는 `npm test`를 실행한다.
- Supabase SQL/Edge Function/Pages 배포가 필요한 작업은 코드 완료와 실제 배포 단계를 분리해서 기록한다.
- 검증용 데이터가 생성되면 가능한 한 정확한 row ID 기준으로 정리한다.

## 역할

### 1. 총괄 디렉터 Agent

- 전체 과업을 관리한다.
- Subagent에게 작업을 할당하고 결과를 취합한다.
- 반려된 Task를 적절한 Agent에게 재할당하거나 최종 보류한다.
- 사용자에게 필요한 질문만 정리해서 전달한다.
- 최종적으로 작업 상태, 테스트, 리스크, 배포 필요 여부를 정리한다.

### 2. 기획 Agent

- 유저 페이지와 관리자 페이지를 서비스 관점에서 검토한다.
- 개선/추가 기능 후보를 정의한다.
- 각 후보에 목적, 기대 효과, 사용자 영향, 운영 영향, 구현 난이도를 붙인다.
- 화면 흐름이나 인터페이스 판단이 필요한 후보는 UX/UI Agent에게 별도 검토를 요청한다.

### 3. UX/UI Agent

- 유저 페이지와 관리자 페이지를 UX/UI 관점에서 검토한다.
- 화면 정보 구조, 흐름, 문구, 모바일 사용성, 접근성, 시각적 밀도, 조작 편의성을 확인한다.
- 기획 Agent가 제안한 기능이 실제 화면에서 자연스럽게 들어갈 수 있는지 검토한다.
- 개발 전에 와이어 수준의 배치 방향과 수동 검증 기준을 제안한다.
- 카드/섹션/폼/테이블이 기존 디자인 시스템과 어긋나지 않는지 확인한다.

### 4. 검토 Agent

- 기획 Agent의 제안을 검토한다.
- 기능을 `P0`, `P1`, `P2`, `P3`, `보류`, `폐기`로 분류한다.
- 사용자 가치, UX/UI 영향, 운영 리스크, 구현 비용, 기존 TODO와의 관계를 기준으로 판단한다.

### 5. 개발 Agent

- 검토 Agent가 승인한 Task를 우선순위 순서로 개발한다.
- 파일 소유 범위를 명확히 받고 작업한다.
- 다른 Agent가 만든 변경을 되돌리지 않는다.
- 변경 파일과 구현 요약을 보고한다.

### 6. QA Agent

- 개발 Agent의 결과를 로컬에서 검증한다.
- `npm test` 결과와 필요한 수동 검증 항목을 기록한다.
- 실제 브라우저 확인이 필요한 경우 대상 URL과 확인 기준을 제시한다.

### 7. 보안 Agent

- 승인된 기능과 구현 결과를 보안 관점에서 검토한다.
- 인증, 권한, RLS, Edge Function, 결제, 입력값 검증, XSS, 데이터 노출을 우선 확인한다.
- 차단 이슈와 권장 개선을 구분해서 보고한다.

### 8. 작업 정리 Agent

- 각 Task의 상태 변경을 `AGENTIC_WORK_LOG.md`에 기록한다.
- 커밋, 테스트, 배포 필요 여부, 보류 사유, 질문을 정리한다.
- `TODO.md`를 직접 대체하지 않고 필요한 경우 TODO 업데이트 제안만 남긴다.

### 9. 보조 Agent

- 특정 Agent가 추가 조사, 문서화, 리팩터링, 테스트 보강이 필요할 때 보조한다.
- 독립적으로 끝낼 수 있는 작은 작업을 맡는다.

### 10. 질문 Agent

- 각 Agent가 작업 중 발견한 질문을 모은다.
- 중복 질문을 합치고, 총괄 디렉터 Agent에게 전달한다.
- 사용자의 결정이 없으면 진행하기 위험한 질문만 남긴다.

## Task 상태

- `proposed`: 기획 또는 탐색으로 제안됨
- `approved`: 검토를 통과해 개발 가능
- `assigned`: 특정 Agent에게 할당됨
- `in_progress`: 개발 또는 검증 진행 중
- `needs_review`: 개발 완료 후 검토 필요
- `rejected`: 반려됨
- `deferred`: 지금은 보류
- `done_local`: 로컬 개발/테스트 완료
- `deployed`: 실제 배포/원격 적용 완료

## 우선순위 기준

- `P0`: 결제, 데이터 무결성, 인증/권한, 배포 실패 등 즉시 위험
- `P1`: 운영 안정성, 관리자 효율, 사용자 핵심 흐름 개선
- `P2`: 접근성, 사용성, 모바일 UX, 테스트 보강
- `P3`: 유지보수, 성능, 문서 정리, 리팩터링

## 작업 진행 절차

1. 총괄 디렉터 Agent가 현재 코드/TODO/로그를 확인한다.
2. 기획 Agent가 기능 후보를 제안한다.
3. UX/UI Agent가 화면 흐름, 사용성, 접근성, 배치 리스크를 검토한다.
4. 검토 Agent가 우선순위와 승인/보류/폐기를 결정한다.
5. 보안 Agent가 고위험 후보를 선검토한다.
6. 개발 Agent가 승인된 Task를 구현한다.
7. QA Agent가 로컬 테스트와 수동 검증 기준을 확인한다.
8. UX/UI Agent가 구현 결과의 화면 품질을 필요 시 재검토한다.
9. 보안 Agent가 구현 결과를 다시 확인한다.
10. 작업 정리 Agent가 로그를 업데이트한다.
11. 총괄 디렉터 Agent가 사용자에게 결과와 다음 선택지를 보고한다.

## 반려 및 재할당 규칙

- 요구사항 불명확: 질문 Agent로 보내고 사용자 결정 전 보류한다.
- 구현 결함: 개발 Agent에게 재할당한다.
- 사용자 가치 부족: 검토 Agent가 `deferred` 또는 `rejected`로 분류한다.
- 화면 흐름 또는 사용성 결함: UX/UI Agent에게 재검토를 맡기고 개발 Agent에게 수정 요청한다.
- 보안 리스크 큼: 보안 Agent가 차단 사유를 기록하고 개발 Agent에게 수정 요청한다.
- 테스트 실패: QA Agent가 재현 조건을 기록하고 개발 Agent에게 재할당한다.
- 배포 리스크 큼: 총괄 디렉터 Agent가 배포를 보류하고 롤아웃 순서를 재정의한다.

## 기록 규칙

- Agentic 진행 기록은 `AGENTIC_WORK_LOG.md`에 남긴다.
- 제품 백로그와 우선순위는 `TODO.md`에 남긴다.
- 로그에는 최소한 다음을 기록한다.
  - 날짜
  - Task ID
  - 담당 Agent
  - 상태
  - 변경 파일
  - 테스트 결과
  - 보안/QA 메모
  - 배포 필요 여부
  - 사용자에게 필요한 질문

## 배포 규칙

- 배포는 사용자가 명시적으로 요청할 때만 진행한다.
- Supabase 원격 SQL 또는 Edge Function 배포는 코드 커밋과 분리해서 기록한다.
- GitHub Pages 배포가 필요한 경우 workflow 성공과 실제 URL 200 확인을 기록한다.
- 검증용 데이터는 생성 ID를 기록하고 정리 여부를 남긴다.
